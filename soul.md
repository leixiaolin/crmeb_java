# soul.md

## 项目定位

本项目基于 CRMEB Java 开源商城二次开发，目标是构建校园外卖小程序 MVP。

首期不是重写一套外卖系统，而是在现有 CRMEB 的商城、商品、用户、支付、订单、后台能力上，增量扩展校园外卖能力。

首期交付范围：

- 微信小程序用户端
- 现有 admin 后台内嵌商家/运营管理
- 不开发独立骑手端
- 不支持多商家合并下单
- 不重构 CRMEB 核心商城主流程

## 核心原则

### 1. 外卖订单必须和普通商城订单隔离

所有订单必须区分订单类型：

- `order_type = 0`：普通商城订单
- `order_type = 1`：校园外卖订单

普通商城订单继续走原有 CRMEB 逻辑。

校园外卖订单可以复用主订单表的支付、金额、用户、退款等基础能力，但外卖专属状态、配送信息、校园地址快照等必须放入扩展表，避免污染普通商城订单模型。

### 2. 外卖状态机独立维护

外卖订单不强行复用商城的“待发货、待收货”语义。

外卖状态建议为：

- `0`：待付款
- `10`：待接单
- `20`：配送中
- `30`：已送达
- `40`：已完成
- `-10`：已取消
- `-20`：退款中
- `-30`：已退款

页面展示、商家操作、定时任务、退款判断，均以外卖扩展状态为准。

### 3. 商家数据隔离是底线

所有商家后台可查询或可操作的数据，必须能稳定关联到 `mer_id` 或等价商家标识。

商家账号只能访问自己名下的数据。

所有写操作必须二次校验数据归属，例如：

- 接单
- 拒单
- 商品上下架
- 库存维护
- 退款处理
- 评价回复
- 配送状态修改

不能直接信任前端传入的订单 ID、商品 ID、退款 ID。

### 4. 校园地址不要改造 UserAddress 主模型

CRMEB 原有 `UserAddress` 面向普通省市区收货地址，不适合作为校园宿舍地址主模型。

校园外卖使用独立地址模型，包含：

- 学校
- 校区
- 楼栋
- 楼层
- 宿舍号
- 联系人
- 手机号
- 默认地址
- 备注

创建订单时必须写入地址快照，避免用户后续修改地址影响历史订单。

### 5. 配送费必须可解释、可测试、可追溯

配送费计算规则：

```text
配送费 = 楼层基础配送费 + 雨天附加费 - 会员卡减免
```

订单实付：

```text
实付金额 = 商品金额 - 优惠金额 + 配送费
```

楼层配送费必须来自配置表。未配置楼层配送费时，不允许下单，不在运行时自动推导费用。

雨天附加费只影响新创建订单，已创建订单不追补、不变更。

### 6. 首期只允许同店下单

购物车可以展示多个商家的商品分组，但结算时只能选择一个商家的商品。

后端创建订单时必须校验所有商品属于同一商家。

该限制用于降低首期支付、退款、优惠、配送费、库存、商家接单的复杂度。

### 7. 库存扣减以支付成功为准

首期采用支付成功后扣减库存。

规则：

- 创建待付款订单时不扣库存
- 支付成功回调中扣减库存
- 扣减失败时订单进入异常状态，由后台处理退款
- 待付款取消不回滚库存
- 接单前取消或商家拒单需要回滚库存

### 8. 现有商城定时任务不得误处理外卖订单

所有现有普通商城订单定时任务，必须增加 `order_type = 0` 过滤。

例如：

- 自动取消未支付订单
- 自动收货
- 自动完成
- 售后/退款相关任务

校园外卖订单使用独立定时任务。

### 9. 外部服务失败不能阻塞主链路

以下能力允许降级：

- 短信通知
- 飞鹅打印机
- 微信订阅消息
- 站内提醒

订单创建、支付、接单、退款等核心链路不能因为非核心通知失败而整体失败。

失败信息需要记录日志或后台可查。

## MVP 优先级

### P0 必须完成

- 外卖订单类型隔离
- 外卖订单扩展表
- 外卖状态机
- 校园地址表
- 学校、校区、楼栋、楼层配送费配置
- 同店下单校验
- 配送费计算
- 商家接单/拒单
- 外卖订单列表和详情
- 支付成功后进入待接单
- 外卖定时取消与自动完成
- 商家数据隔离

### P1 可以随后完成

- 雨天附加费配置
- 会员卡配送费减免
- 飞鹅打印机
- 微信订阅消息
- 订单超时提醒
- 运营统计

### P2 暂不做

- 独立骑手端
- 多商家合并下单
- 智能派单
- 路线规划
- 多校区复杂配送调度
- 商家独立 APP

## 技术边界

后端继续使用：

- Java 8
- Spring Boot 2.2.6.RELEASE
- Maven 多模块
- MyBatis-Plus
- MySQL
- Redis

后端改动应遵循现有分层：

```text
Controller -> Service -> DAO/Mapper
```

不要把业务逻辑堆在 Controller。

管理后台继续使用：

- Vue 2
- Element UI
- Vuex
- Vue Router

移动端继续基于现有 `app/` uni-app 工程。

## 验收标准

首期验收时，至少要验证：

- 普通商城订单不受外卖改动影响
- 外卖订单不会被普通商城定时任务误处理
- 商家只能看到自己的商品和订单
- 用户只能同店下单
- 未配置楼层配送费时无法下单
- 支付成功后库存扣减
- 商家拒单后退款和库存回滚逻辑正确
- 外卖订单状态流转符合设计
- 配送费、雨天费、会员减免有订单快照
